generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model dailycontent {
  id                     Int      @id(map: "DailyContent_pkey") @default(autoincrement())
  language               String
  level                  String
  contentDate            DateTime @db.Date
  wordForeign            String
  wordNative             String
  grammarExplanation     String?
  grammarExample         String?
  grammarTranslationCz   String?
  grammarTranslationOrig String?
  readingForeign         String?
  readingNative          String?
  wordExampleForeign     String?
  wordExampleNative      String?
  grammarFamily          String?
  grammarPattern         String?
  grammarContext         String?
}
